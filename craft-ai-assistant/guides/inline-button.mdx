---
sidebar_position: 1
---

import { Photo } from '@site/src/components/utils';
import { VerticalStepWrapper, StepMarkdown } from '@site/src/components/docs';

# Inline Button

This guide will help you quickly start generating content with AI Assistant using the inline **AI** button directly inside supported fields when editing entries.

<Photo
  img={require('@site/static/img/craft/ai-assistant/v1/cp/entry-highlight.jpg')}
  alt="Using the Inline AI Button"
/>

<Photo
  img={require('@site/static/img/craft/ai-assistant/v1/cp/generate-text.jpg')}
  alt="Generate Text with AI Assistant"
/>

<Photo
  img={require('@site/static/img/craft/ai-assistant/v1/cp/generate-image.jpg')}
  alt="Generate Images with AI Assistant"
/>

### Setup Instructions

<VerticalStepWrapper>
  <StepMarkdown>
    Open an entry with AI-enabled fields. The inline AI button will appear inside the input.
  </StepMarkdown>
  <StepMarkdown>
    Click the button to open the **Generate** modal.
  </StepMarkdown>
  <StepMarkdown>
    Choose a prompt and adjust the **Prompt Text** if needed.
  </StepMarkdown>
  <StepMarkdown>
    (Optional) Enable **Include input context** to use existing field content.
  </StepMarkdown>
  <StepMarkdown>
    Click **Generate** and review the result.
  </StepMarkdown>
  <StepMarkdown>
    When you're satisfied with the result, click **Insert** to replace the field content.
  </StepMarkdown>
</VerticalStepWrapper>
