---
title: 'Color Picker'
---

import { Badge, BadgeGroup, Photo } from '@site/src/components/utils';

The `#[ColorPicker]` property type will render a color picker in the field properties panel.

```php showLineNumbers
use Solspace\Freeform\Attributes\Property\Input\ColorPicker;
// ...

#[ColorPicker(
    label: 'Background Color',
    instructions: 'Select a background color for the field.',
)]
protected string $backgroundColor = '#ffffff';
```

#### Result
<Photo
  img={require('@site/static/img/craft/freeform/v5/developer/field-properties/color-picker.png')}
  alt="Color Picker Property Editor Preview"
  unscaled
/>
